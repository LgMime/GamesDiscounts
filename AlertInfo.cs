using Save;

namespace GamesDiscounts
{
    public class AlertInfo
    {
        //public async Task SendDailyAlerts(long chatId)
        //{
        //    GameInfo gameInfo = new GameInfo();
        //    var gameAlerts = gameInfo.FoundAlertGames(chatId);
        //    foreach (var game in gameAlerts)
        //    {
        //        await gameInfo.FoundGameAppIdAsync(game, true);
        //    }
        //}

    }
}